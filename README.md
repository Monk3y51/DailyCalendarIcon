# DailyCalendarIcon
Download Google Calendars icon and set the chrome PWA to match the current date, designed to work with KDE.

## How to setup
1. Clone repository
2. run the setup.sh

The script will run at login to update the icon, currently it will not update the icon if the system is logged in at midnight, looking to introduce later on.
